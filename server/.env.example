# 이 파일을 .env 로 복사해서 실제 값을 넣는다. .env 는 커밋되지 않는다.
# Neon 은 반드시 -pooler 붙은 풀링 주소를 쓴다. 무료 인스턴스는 연결 수가 적다.
DATABASE_URL=postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/neondb?sslmode=require
PORT=3000
